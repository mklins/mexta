<nav class="navbar sticky-top navbar-expand bg-light">
  <div class="container-fluid">
    <div class="navbar-collapse d-flex collapse">
      <ul class="navbar-nav col-6 ps-3 justify-content-start">
        <li class="nav-item">
          <%= link_to 'Spendings', spendings_path,
            class: "nav-link #{'active' if current_page?(spendings_path)}" %>
        </li>
        <li class="nav-item">
          <%= link_to 'Categories', categories_path,
            class: "nav-link #{'active' if current_page?(categories_path)}" %>
        </li>
      </ul>
      <% if user_signed_in? %>
        <div class="d-flex col-6 pe-3 justify-content-end">
          <%= link_to 'Sign Out', destroy_user_session_path,
            class: 'btn btn-outline-primary', data: { "turbo-method": :delete } %>
        </div>
      <% end %>
    </div>
  </div>
</nav>
